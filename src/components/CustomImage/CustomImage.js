
import React from 'react'

const CustomImage = ({image}) => {
    return (
      
        <img src={image} style={{height: '160px', width: '100%'}} alt="image" />
      
    );
}
 export default CustomImage